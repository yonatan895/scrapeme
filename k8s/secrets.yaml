# Secrets for ScrapeMe application
# WARNING: This is a template file with example values
# In production, replace with actual base64-encoded secrets
# Generate base64 values: echo -n "your-value" | base64

apiVersion: v1
kind: Secret
metadata:
  name: scrapeme-secrets
  namespace: scrapeme
  labels:
    app.kubernetes.io/name: scrapeme
    app.kubernetes.io/component: configuration
type: Opaque
data:
  # Example values - REPLACE WITH REAL VALUES
  SITE_USERNAME: ZXhhbXBsZS11c2Vy  # base64 for "example-user"
  SITE_PASSWORD: ZXhhbXBsZS1wYXNzd29yZA==  # base64 for "example-password"
  GRAFANA_ADMIN_PASSWORD: YWRtaW4xMjM=  # base64 for "admin123"
  
  # Additional secrets (optional)
  API_KEY: ""  # Add if needed for external APIs
  DATABASE_URL: ""  # Add if using external database
  WEBHOOK_SECRET: ""  # Add for webhook authentication

---
# TLS Secret template (uncomment when ready to use HTTPS)
# apiVersion: v1
# kind: Secret
# metadata:
#   name: scrapeme-tls
#   namespace: scrapeme
# type: kubernetes.io/tls
# data:
#   tls.crt: |  # base64-encoded certificate
#   tls.key: |  # base64-encoded private key

---
# Docker registry secret template (uncomment if using private registry)
# apiVersion: v1
# kind: Secret
# metadata:
#   name: docker-registry-secret
#   namespace: scrapeme
# type: kubernetes.io/dockerconfigjson
# data:
#   .dockerconfigjson: |  # base64-encoded Docker config JSON
